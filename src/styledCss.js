import { css } from 'styled-components';

export default css`
  --primary: #0066cc;
  --navBarHeight: 80px;

  body {
    overflow-x: hidden;
  }

  .container {
    width: 100%;
    padding-left: 16px;
    padding-right: 16px;
    margin-left: auto;
    margin-right: auto;
    max-width: 1200px;
  }
`;
